# Ingestion service: API (upload/process) + worker (yt-dlp, pydub)
fastapi
uvicorn
python-multipart
yt-dlp[default]   # [default] bundles yt-dlp-ejs — YouTube's EJS challenge solver scripts
pydub
audioop-lts  # pydub on Python 3.13 (audioop removed from stdlib)
boto3
python-dotenv
